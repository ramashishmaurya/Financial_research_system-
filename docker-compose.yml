version: '3.8'

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    restart: always

# Hum abhi PostgreSQL use nahi kar rahe kyunki default SQLite pe chal raha hai.
# Jab AWS RDS pe jayenge tab Postgres add kar denge.
